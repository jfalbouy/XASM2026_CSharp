# Portage C# natif

## Reference

Les sources C a respecter sont conservees dans `Reference/C`.

```text
xasm.c      boucle principale, passes, options, fichiers
var.c       etat global
init.c      tables de caracteres et hash initial
hash.c      symboles, mnemonics, macros
eval.c      evaluation d'expressions
opr.c       parsing d'operandes
mvopr.c     encodage MV/EX
genop.c     directives et dispatch opcodes
misc.c      helpers, listing, emission objet
modern.c    sorties 2026 et directives ajoutees
mes.c       messages, erreurs, usage
```

## Cibles C#

```text
Core/        modeles simples et etat d'assemblage
Parsing/     lecture source, tokens, operandes
Symbols/     labels, hash, scopes, macros
Expressions/ evaluation numerique
Instructions/dispatch opcodes et encodage
Directives/  ORG, EQU, INCLUDE, MACRO, IFDEF, REPEAT, STRUCT, SECTION
Outputs/     objets, listing, Intel HEX, S19, MAP, D, UU, HxD
```

## Regles de non-regression

Les sorties suivantes doivent etre comparees avec `xasm2026-1` :

```text
.obj
.lst
.hex
.s19
.map
.d
.uu
.txt
```

Les exemples prioritaires sont :

```text
Exemples/SAMPLES/SAMPLE5.ASM
Exemples/VOGUE/VOGUE.S
Exemples/REGISTER/REGISTER.ASM
Exemples/TMAP/TMAP2020.asm
```

## Journal

- 2026-06-03 : creation de `xasm2026-3`.
- 2026-06-03 : copie de la documentation, des exemples et des sources C de reference.
- 2026-06-03 : creation du projet C# natif.
- 2026-06-03 : port initial du parsing des options, Intel HEX, S-Record et dump HxD.
- 2026-06-03 : premier moteur assembleur C# autonome : `ORG`, `END`, `EQU`, `SECTION`, `STRUCT/ENDS`, `REPEAT/ENDR`, `IFEQ/IFNE/IFGT/IFLT`, `DB/DM/DW/DP/DS`, `NOP` et formes `MV` utilisees par `SAMPLE5`.
- 2026-06-03 : verification `SAMPLE5` : sorties `.hex` et `.s19` identiques a la reference.
- 2026-06-03 : port du generateur BASIC uuencode auto-decodable `-B`; verification de creation de `SAMPLE5.native.uu`.
- 2026-06-03 : branchement de `-L`, `-S`, `-C`, `-E`, lecture `INCLUDE` et dependances enrichies.
- 2026-06-03 : verification complete `SAMPLE5` avec `-O -L -E -S -C -TZ -I -M -P -D -B -X -R -W -V -H`; tous les fichiers attendus sont generes, `.hex` et `.s19` restent identiques a la reference.
- 2026-06-03 : test `VOGUE.S`; les includes et directives simples passent, blocage attendu sur `JP`, premier opcode SC62015 complet non encore porte.
- 2026-06-03 : amelioration du message d'erreur console; la commande `bin\xasm2026-3.exe vogue.s -O vogue.obh -L vogue.lst -B vogue.uu` indique maintenant `ligne 120: start: jp main`.
- 2026-06-03 : port partiel supplementaire pour `VOGUE.S` : sauts/appels `JP/JPF/JP*/CALL/CALLF/JR*`, `PUSHU/POPU/PUSHS/POPS`, `RET/RETF`, operations immediates sur `A`, `INC/DEC`, et premieres formes `MV/MVP/MVW`. Le test avance maintenant jusqu'a `sub x,y`.
- 2026-06-03 : reprise du port `VOGUE.S` : formes `ADD/SUB` internes, `MV/MVW/MVP/MVL` indexees, registres `IL/BA/I/X/Y/U/S`, rotations/decalages, `ADCL/SBCL`, `EX/EXW/EXP/EXL`, `SWAP`, labels locaux imbriques et references parent `..!`.
- 2026-06-03 : correction de la lecture des fichiers inclus : les `END` internes ne terminent plus l'assemblage global.
- 2026-06-03 : verification complete `SAMPLE5.ASM` avec toutes les options de sortie : OK, code `00E000h - 00E01Dh` et fichiers generes.
- 2026-06-03 : finalisation des encodages SC62015 utilises par `VOGUE.S` : piles `PUSHS/POPS`, `JP X`, lectures/ecritures internes hautes, acces `PX`, formes `MV/MVP` indexees et comparaison `CMP (bp+...),A`.
- 2026-06-03 : verification complete `VOGUE.S` avec `-O -L -E -S -C -TZ -I -M -P -D -B -X -R -W -V -H` : OK sans erreur fatale, fichiers generes. Le code produit couvre `0B9800h - 0BD060h`, soit 14433 octets, identique a la reference historique. La carte des symboles ne presente plus aucun ecart d'adresse avec `xasm2026-1`.
- 2026-06-03 : alignement octet a octet de `VOGUE.S` : corrections des formes `ADD/SUB A,(...)`, `AND/OR/XOR A,(...)`, `MV [(bp+...)+n],(...)`, `MVP (...),[adresse]` et evaluation des expressions avec `*` et `/`. Les sorties `VOGUE.hex`, `VOGUE.s19`, `SAMPLE5.hex` et `SAMPLE5.s19` sont identiques aux references.
- 2026-06-03 : correction du format `.lst` C# : le listing n'est plus un dump texte du binaire, il reprend une presentation assembleur ligne par ligne avec adresse, octets generes, commentaires, labels, directives et instructions. Verification `SAMPLE5.ASM` et `VOGUE.S` OK, sans regression sur `.hex` et `.s19`.
- 2026-06-03 : correction de l'entete BASIC `.uu` : les lignes 290 a 410 utilisent maintenant un seul marqueur `%` apres l'apostrophe, conformement au format attendu. Verification OK sur `SAMPLE5.check.uu` et `vogue.check.uu`.
- 2026-06-03 : dernier check croise `xasm2026-2` / `xasm2026-3` avec toutes les options sur `SAMPLE5.ASM` et `VOGUE.S`. Les sorties machine `.hex` et `.s19` sont identiques pour les deux programmes. Les differences restantes concernent les formats de presentation ou d'emballage (`.lst`, `.map`, `.d`, `.txt`, `.uu`) et le type objet `-TZ`, que `xasm2026-3` emet au format texte ZSH.
- 2026-06-04 : alignement final avec les sorties DOSBox `C:\sharp\xasm140-0\Exemples\VOGUE`. `vogue.obj`, `vogue.lst` et `vogue.uu` produits par `xasm2026-3` sont maintenant strictement identiques aux references. Le generateur `.uu` reproduit le comportement de `uuselfx.c` : tampon de 45 octets conserve sur le dernier bloc, checksum historique et ligne finale `size`. Le listing utilise le decoupage historique de 6 octets par ligne, conserve les `end` des fichiers inclus, et reprend le resume final `Code: ...`.
- 2026-07-18 : les formats de presentation entrent dans la non-regression octet a octet. L'invocation historique ayant produit les goldens a ete reconstituee : noms de source et de sorties entierement en minuscules (`sample5.asm` -> `sample5.lst`, ...) et option `-S` (table des symboles annexee au listing). Sous cette invocation, `SAMPLE5`, `VOGUE`, `REGISTER` et `TMAP2020` produisent des `.lst`, `.map`, `.d` et `.uu` strictement identiques aux references (32 sorties sur 32, en comptant `.obj/.hex/.s19/.txt`). Les ecarts observes auparavant venaient uniquement de la casse du nom de fichier embarque et de l'absence de `-S`. Le harnais `GoldenAssemblyTests` compare desormais les huit formats, en neutralisant le seul champ non deterministe : la ligne `' Submitted jj/mm/aaaa` du `.uu`. Les goldens de presentation encore issus de `Publish XASM2026-3 final project` (produits sans `-S` par un build qui n'echoait pas les lignes vides du source) ont ete remplaces par les sorties de reference alignees sur DOSBox le 2026-06-04.
- 2026-07-18 : l'option `-W` devient effective. Elle etait jusqu'ici analysee dans `CommandLineOptions` puis jamais lue. Quatre des six avertissements non fatals de `mes.c` (`err_handle`) sont portes, avec leur libelle historique : 28 `Location counter already set` (ORG apres origine deja fixee, `genop.c` case 64), 32 `No effective code` (`DS 0`, case 73), 33 `LOCAL and ENDL not match in included file` et 38 `PRE_PUSH and PRE_POP not match` (deseequilibre constate a la fermeture d'un fichier inclus, case 66). Les codes 29 (`Used PRE while auto-prebyte is active`) et 34 (`INCLUDE argument isn't defined yet`) ne sont pas portes : ils supposent respectivement la directive de donnees `PRE` et les arguments de `INCLUDE`, absents du port ; a ajouter avec ces fonctionnalites. Semantique conforme au C : un avertissement ne rend jamais l'assemblage fatal et reste muet sans `-W`, ce qui garantit l'absence d'effet sur les goldens (aucun n'a ete produit avec `-W`). Rendu console `fichier<TAB>ligne<TAB>texte`, annexe au `.lst` et au `.err` quand ils sont demandes. Divergences assumees, toutes liees a l'absence de correspondance avec les lignes physiques : le C intercale l'avertissement a la ligne fautive du listing (on les regroupe en fin), le C ajoute `col N` sous `-V` (on n'instrumente pas la colonne et on prefere ne rien afficher), et les numeros rapportes pour le source principal sont ceux des lignes developpees. Verifie : aucun avertissement parasite sur `SAMPLE5`, `VOGUE`, `REGISTER`, `TMAP2020` et `coverage_all`, et non-regression octet a octet inchangee (9 tests verts).
- 2026-07-18 : numeros de ligne physiques (amelioration (c)), banniere `title` et correction de `-?`. (1) Chaque ligne source porte desormais son origine via le nouveau type `src/Assembly/SourceRef.cs` (`Text`, `File`, `Line`). L'origine est attachee a la ligne elle-meme et non a une liste parallele, parce que les corps de MACRO et les blocs REPEAT sont copies puis rejoues, ce qui detruirait toute correspondance positionnelle ; c'est le pendant de `file_typ.name` / `file_typ.lines` du C. Les erreurs et avertissements designent maintenant le fichier reel : une faute dans un INCLUDE est rapportee `ligne N (fichier.asm): ...`, la forme historique `ligne N: ...` etant conservee quand la faute est dans le source principal. Les lignes issues d'une expansion de macro sont rattachees au **site d'appel**, comme dans le C ou `current_file->lines` vaut la ligne en cours de lecture au moment du rejeu. `Program.TryParseErrorLine` accepte les deux formes et relit la ligne fautive dans le fichier qui la contient reellement. Les deux divergences restantes sur les warnings sont l'intercalage dans le listing (on les regroupe en fin) et l'absence de `col N` sous `-V`. (2) Ajout de `Usage.WriteTitle`, pendant de `title()` de `mes.c`, avec la constante `Usage.Version` (pendant du `#define VERSION` de `xasm.h`) : la banniere reprend l'encadrement et les credits historiques et ajoute la mention du portage 2026. (3) Correction de `-?` : `CommandLineOptions.Parse` prenait `args[0]` pour le fichier source sans condition et n'analysait les options qu'a partir de l'indice 1, si bien qu'une option placee en premier n'etait jamais vue (`xasm -?` cherchait un fichier `-?.asm`). Le premier argument n'est desormais traite comme source que s'il ne commence pas par `-`. `-?` est aussi ajoute a la liste des options affichees. Verifie : 13 tests verts, non-regression octet a octet inchangee.
- 2026-07-18 : suppression des chemins machine `C:\Codex\...`. `tools/build_documentation.py` deduit desormais sa racine de l'emplacement du script (`Path(__file__).parent.parent`) ; ses deux ressources externes absentes du depot (table d'instructions PC-E500, `XASM140 - Information`) restent referencees par defaut mais sont surchargeables via les variables d'environnement `XASM_INSTRUCTION_TABLE` et `XASM140_INFO_TXT`, `XASM_FINAL_ROOT` couvrant la racine de publication. `tools/compare_with_xasm2026_1_1.ps1` deduit `OutDir` et `CandidateXasm` de `$PSScriptRoot`, accepte l'exe de reference via `-ReferenceXasm` ou `XASM_REFERENCE_EXE` et echoue avec un message explicite s'il manque, la liste d'exemples etant devenue relative et tolerante aux entrees absentes (TRDOS et UUCODE ne font pas partie de ce checkout). `src/Properties/launchSettings.json` utilise des `workingDirectory` relatifs au dossier du projet, les profils de lancement Visual Studio etant jusqu'ici inutilisables hors de `C:\Codex`. Les documents `Documentation/*.md` ont perdu leurs chemins machine au profit de chemins relatifs a la racine du projet. Enfin, les deux `.docx` portaient un nom en 2026-4 pour un contenu entierement en 2026-3 : leur regeneration etant impossible ici (python-docx absent et sources externes manquantes), leurs parties XML ont ete reecrites en place, ce qui est sur car aucune des occurrences visees n'etait fractionnee entre plusieurs runs XML (verifie au prealable) et la mise en forme est integralement preservee. Verifie : archives ZIP integres, `[Content_Types].xml` en tete, 0 occurrence residuelle de `2026-3` ou de `Codex`.
- 2026-07-18 : les avertissements sont desormais intercales dans le listing a la ligne fautive, comme le fait `err_handle` dans le C, au lieu d'etre regroupes en fin de fichier. L'appariement se fait par l'origine physique : `ListingLine` porte maintenant les champs `File` et `Line`, et `ListingWriter` emet apres chaque ligne les avertissements dont l'origine correspond, en preservant leur ordre lorsque plusieurs visent la meme ligne (cas de 33 et 38 sur la derniere ligne d'un fichier inclus). Les avertissements sans ligne de listing correspondante sont vides en fin de fichier plutot que perdus. L'option `-V` retrouve sa colonne : `AssemblyWarning.Column` designe le debut 1-base de l'operande fautif, ou du mnemonique a defaut. Ce n'est volontairement pas le `pp` du C, qui est la position courante de l'analyseur au moment de l'erreur ; c'est une approximation stable et verifiable, documentee comme telle. Le formatage est centralise dans `AssemblyWarning.Format`, pendant du `errtext` unique de `err_handle`, pour que console, `.lst` et `.err` ne puissent pas diverger. Sans `-W` le listing ne porte aucune trace d'avertissement, ce qui garantit l'invariance des sorties de reference. Verifie : intercalage et colonnes controles sur un cas couvrant les quatre avertissements, 15 tests verts.
- 2026-07-18 : extraction de vrais types depuis le noyau. `src/Assembly/SymbolTable.cs` porte desormais les valeurs de symboles, les occurrences d'adresse et les regles de portee locale (prefixe `scope!label`, reference parente `..!`), qui etaient jusqu'ici quatre champs et cinq methodes disperses dans le partial `NativeAssembler` ; c'est le pendant de `hash.c` et de la pile `l_stack` du C. Cette partie etant la plus subtile du port, elle gagne une suite de tests unitaires dediee (`SymbolTableTests`, 18 cas) qui couvre le prefixage, l'imbrication et le devidage des portees, la resolution de `..!`, l'ordre des candidats de resolution et l'insensibilite a la casse. `src/Assembly/RegisterTable.cs` regroupe les tables registre/opcode SC62015, qui sont sans etat : le noyau les importe via `using static`, donc aucun des 36 appels n'a ete modifie. Le dispatch directive/opcode reste volontairement un `switch` de 96 cas et n'est **pas** converti en table : C# compile deja un switch sur chaine en saut indexe par hachage, une table n'apporterait ni vitesse ni lisibilite, et reecrire les 96 branches est precisement le type de changement que la contrainte d'exactitude octet a octet rend couteux a valider. Verifie : 33 tests verts, non-regression octet a octet inchangee sur les 4 exemples et les 8 formats.
- 2026-07-18 : portage des bases numeriques et du compteur de localisation dans l'evaluateur d'expressions, conformement a `eval.c`. (1) C'est desormais le **dernier caractere** du jeton qui fixe la base : `B`=2, `O`=8, `D`=10, `H`=16 ; sans suffixe reconnu la base est 10 et ce caractere est un chiffre a part entiere. Les suffixes sont acceptes dans les deux casses, le souligne `_` est un separateur visuel ignore (`1010_1010B`, `0F_FH`, `1_000`), et un chiffre superieur ou egal a la base invalide le jeton (err 23 du C), qui remonte alors comme symbole indefini plutot que d'etre silencieusement mal converti. Un nombre doit commencer par un chiffre ou par `$`, ce qui preserve la distinction avec les noms de symboles et explique l'exigence du zero de tete en hexadecimal (`0FFH` et non `FFH`). L'ancien `IsHexToken`, devenu mort, est supprime. (2) L'operande `*` rend le compteur de localisation. Les deux emplois de `*` se distinguent par la position dans l'analyse, ce qui est l'equivalent exact du drapeau `set_x` du C : en position de terme il vaut le compteur, entre deux valeurs il reste l'operateur de multiplication ; `**2` vaut donc « compteur x 2 ». Le compteur est transmis a chaque evaluation, pendant de la globale `lc` du C lue au moment de l'evaluation, si bien qu'il avance au fil des octets emis a l'interieur d'une meme directive. Verifie : octets generes `B5 FF D2 FF AB AA 2A` pour les six notations et le defaut, `dp *` rendant successivement `00E000` puis `00E003`, `db 4*3` valant toujours `0C` ; 59 tests verts dont 26 nouveaux, non-regression octet a octet inchangee.
- 2026-07-18 : portage des operateurs binaires, des arguments d'INCLUDE et de la directive PRE, ce qui rend au passage les six avertissements de `mes.c` atteignables. (1) Operateurs `%`, `&` et `|` ajoutes a l'evaluateur avec la precedence exacte de `oprlevel_set` (init.c), du plus faible au plus fort : `|` 3, `&` 4, `%` 5, `+ -` 6, `* /` 7. **Point de vigilance** : le modulo lie moins fort que l'addition dans ce langage, contrairement au C ; `1+2%3` vaut donc `(1+2)%3` = 0 et non `1+(2%3)` = 3, ce que verrouille un test dedie. Les trois caracteres terminent desormais un jeton dans `ReadToken`, sans quoi ils seraient absorbes dans un nom de symbole. (2) `INCLUDE fichier[,arg0,...,arg9]` et les references `@0`..`@9` sont portes. Les INCLUDE etant aplatis avant toute passe, la table des symboles n'est pas encore peuplee au moment de leur lecture : `SourceRef.Args` transporte donc les **expressions d'arguments sous forme de texte**, et la substitution a lieu au moment de l'evaluation (`SubstituteArguments`, avec parenthesage pour preserver la precedence de l'expression injectee). Les `@n` du fichier parent sont substitues dans les arguments avant transmission, si bien qu'un `@n` imbrique designe la bonne portee. Une reference hors plage donne une erreur explicite, pendant de l'err 31. (3) `PRE valeur` emet un seul octet (genop.c case 70 : famille des directives de donnees avec `offset = 1`, `set_reg_opecode` n'ecrivant alors que l'octet bas), avec validation du prebyte dans `21h`-`27h` ou `30h`-`37h` (err 1). (4) Consequence : les avertissements 29 et 34 sont desormais implementes. Le 34 reste peu observable, pour une raison d'architecture documentee et non un defaut : le preprocesseur evalue tous les `EQU` avant les passes, donc un symbole defini plus loin est deja connu et la notion d'ordre du C s'efface ; et si le symbole n'est defini nulle part, l'emploi de `@n` leve d'abord une erreur fatale de symbole indefini, plus utile. Il ne remonte donc que si l'argument fautif n'est jamais utilise. Verifie : octets `FF F0 02 00 02 21 10 03 13` pour un cas couvrant les trois fonctionnalites, avertissements 29 et 34 declenches, erreurs de prebyte illegal et d'argument hors plage correctement situees ; 75 tests verts dont 16 nouveaux, non-regression octet a octet inchangee.
- 2026-07-18 : la division et le modulo par zero deviennent une erreur fatale, pendant de l'err 2 de `mes.c`. Ils rendaient jusqu'ici `0` silencieusement, ce qui produisait un binaire faux sans que rien ne le signale : exactement la classe de defaut que les garde-fous du projet visent. Le partage des responsabilites suit celui deja retenu pour les symboles indefinis : l'evaluateur ne leve pas, il rend `0` et positionne `ExpressionEvaluator.DividedByZero` ; c'est `NativeAssembler.Eval` qui en fait une erreur fatale, et **uniquement en passe d'emission**. Ce dernier point est essentiel : en passe de resolution un diviseur symbolique vaut encore `0`, si bien qu'un controle plus precoce rejetterait un source valide utilisant une reference avant (`db 20H/taille` avec `taille: equ 4` plus loin), cas desormais couvert par un test. Le controle des symboles indefinis passe avant celui de la division : quand le diviseur est un symbole inconnu, le defaut rapporte est le symbole manquant, pas la division. Verifie : `db 10/0` et `db 10%(3-3)` echouent avec `Division by zero` situe sur la bonne ligne et code de retour 1, aucun fichier objet n'est ecrit, `db 10/INCONNU` rapporte bien le symbole, et la reference avant assemble a `08`. 85 tests verts dont 10 nouveaux, non-regression octet a octet inchangee ; aucun exemple de reference n'effectue de division, toutes les occurrences de `/` y etant des litteraux caractere.
- 2026-07-19 : correction d'un defaut silencieux sur les etiquettes de macro. Une macro expansee plusieurs fois avec une etiquette interne et une **reference avant** emettait des adresses fausses sans rien signaler : les sauts des deux expansions se retrouvaient croises (`jp` de l'expansion 1 visant le `fin` de l'expansion 2, et reciproquement). Cause : l'etiquette etait un symbole global redefini a chaque expansion, et en passe d'emission une reference avant lisait encore la valeur laissee par la passe precedente. Origine reelle : une **omission de portage**, pas une fonctionnalite manquante. `genop.c` case 66 montre qu'un `LOCAL` **sans etiquette** ouvre une portee anonyme dont le nom est fabrique (`n%05X` via le compteur `no_name_lbl`) puis enregistre comme un symbole ordinaire a la valeur de `lc` ; le port n'ouvrait de portee que `if (line.Label is not null)`, si bien qu'un `LOCAL` nu ne faisait rien. A noter : l'err 20 (« No label before LOCAL ») n'est **jamais levee** dans le C, son message est du code mort — un `LOCAL` nu est donc parfaitement legitime, et c'est meme l'idiome attendu dans un corps de macro, comme le montre `tests/coverage_all.asm`. Le compteur est remis a zero **a chaque passe**, comme `no_name_lbl` (xasm.c ligne 121, a l'interieur de la boucle de passes), faute de quoi les deux passes fabriqueraient des noms differents et les adresses divergeraient. Sans effet sur les goldens : aucun des quatre exemples de reference n'utilise de `LOCAL` nu (les 147 occurrences de `RUNTIME.S` et `COMPILE.S` sont toutes etiquetees) et aucun `.map` de reference ne contient de symbole `n000xx`. Verifie : le reproducteur emet desormais `02 04 E0 00 00 02 09 E0 00 00`, chaque saut visant sa propre etiquette, contre `02 09 E0 ... 02 04 E0` auparavant ; portees anonymes imbriquees correctes ; `coverage_all.asm` inchange a 892 octets. 88 tests verts dont 3 nouveaux, non-regression octet a octet inchangee.
- 2026-07-19 : lot d'extensions vers un assembleur macro complet, toutes **purement additives** — un source qui ne les utilise pas emet les memes octets, de sorte que les 32 comparaisons octet a octet restent valides par construction. (1) `SET` et sa forme `=` : symbole redefinissable, evalue dans l'ordre du source par le preprocesseur, ce qui rend `REPEAT` reellement generatif (un compteur progresse d'une iteration a l'autre). (2) `IRP nom,v1,v2,...` et `IRPC nom,chaine` : repetition sur liste et sur caracteres, ces derniers injectes comme litteraux entre apostrophes pour que `DB c` emette leur code ASCII. La collecte de blocs est factorisee dans `CollectBlock`, desormais **sensible a l'imbrication** : `REPEAT`, `IRP` et `IRPC` partageant `ENDR`, le premier terminateur fermait auparavant le bloc exterieur, et `REPEAT` n'etait donc pas imbricable. (3) Operateurs `^` (entre `|` et `&`), `~` unaire, decalages `<<` `>>` et comparaisons `= <> < > <= >=` rendant 1 ou 0. Le C s'arretant a `|` dans `oprlevel_set`, leur precedence est une decision de conception : comparaisons au niveau le plus faible, decalages entre le modulo et l'addition afin que `1<<2+3` vaille `1<<5` comme en C ; `<` et `>` ne sont acceptes que s'ils n'amorcent pas un decalage. (4) Extraction d'octets `LOW` / `MID` / `HIGH`, calquee sur `xlow` / `xmid` / `xhigh` de `misc.c`. (5) `ALIGN n` et `EVEN`, qui **emettent** le remplissage car l'image est contigue. (6) `DZ` ; les sequences d'echappement dans `DM` sont volontairement ecartees, elles reinterpreteraient silencieusement les antislashs des sources existantes. (7) `ASSERT` / `ERROR` / `WARNING`, l'assertion n'etant evaluee qu'en passe d'emission pour la meme raison que la division par zero. (8) `PHASE` / `DEPHASE` : les etiquettes prennent l'adresse logique, les octets restent a leur place physique via un decalage retire dans `Emit` ; cas courant sur PC-E500S ou du code est recopie en RAM systeme. (9) Table des **references croisees** sous la nouvelle option `-U`, gated comme `-W` pour ne pas toucher aux goldens. Correction au passage d'une affirmation erronee de la feuille de route : `AddOccurrence` enregistrait les sites de **definition** (pour choisir la cible d'un saut relatif), pas les utilisations ; celles-ci sont desormais collectees par l'evaluateur via `Referenced`. (10) `TITLE`, `LIST` / `NOLIST`, `PAGE` pour la mise en page du listing, `NOLIST` ne changeant jamais les octets emis. (11) Alias `TCP` du mnemonique `TCL` : la table de hachage du C enregistre `TCP` (init.c) alors que son commentaire dit `TCL` (genop.c case 56) ; les deux graphies sont acceptees, faute de quoi `tests/coverage_all.asm` echouerait sur la reference et un source ecrit pour elle echouerait ici. Verifie : 117 tests verts dont 29 nouveaux, non-regression octet a octet inchangee.
- 2026-07-19 : portage de l'err 13 « Duplicate label » (xasm.c). Une etiquette d'adresse deja definie dans la meme portee interrompt desormais l'assemblage. Le controle n'a lieu qu'en **passe de resolution**, comme le `if (pass_sw == 1)` du C, la passe d'emission redefinissant legitimement toutes les etiquettes. Il est sensible aux portees : le meme nom dans deux blocs `LOCAL` distincts reste valide, sans quoi l'idiome des macros deviendrait inutilisable. `EQU` en est exempt parce que le preprocesseur le pre-evalue avant les passes, et `SET` l'est par construction. Le cas reellement attrape est la macro etiquetee expansee deux fois sans `LOCAL`, qui produisait jusqu'ici des adresses fausses en silence. Aucun des quatre exemples de reference n'en contient, ce qui etait attendu puisqu'ils assemblent sur `xasm2026-1`. Au passage, le bloc de definition des etiquettes a ete deplace **a l'interieur du try** qui situe les erreurs : les doublons, mais aussi les erreurs d'evaluation d'un `EQU`, portent maintenant leur position source, y compris a travers un INCLUDE (`ligne 2 (sub.asm): ...`). Verifie : doublon franc, macro sans `LOCAL` expansee deux fois et doublon dans un fichier inclus tous rejetes avec leur ligne ; memes noms dans des portees distinctes acceptes ; `coverage_all.asm` inchange a 892 octets. 122 tests verts dont 5 nouveaux, non-regression octet a octet inchangee.
- 2026-07-19 : `EXITM` et expansion recursive des corps de macro. Jusqu'ici le corps d'une macro etait recopie tel quel dans le flux developpe : les conditionnelles qu'il contenait n'etaient donc resolues qu'a la passe d'assemblage, les `REPEAT` internes n'etaient pas developpes et les appels de macro imbriques restaient lettre morte. Le corps est desormais **re-developpe** par `ExpandSourceBlock`, ce qui rend `EXITM` possible et debloque au passage les macros imbriquees — que le C interdit pourtant categoriquement (err 44 « Cannot use macro in macro ») — ainsi que `REPEAT` a l'interieur d'une macro. Le risque pour la non-regression etait nul et cela a ete verifie avant d'ecrire la moindre ligne : **aucun des quatre exemples de reference n'utilise `MACRO`**. `EXITM` pose un drapeau `_exitMacroRequested` qui remonte a travers les blocs imbriques (`REPEAT`, `IRP`, conditionnelles) jusqu'a la macro englobante, laquelle le consomme ; il ne sort donc pas plus haut que la macro qu'il interrompt, et son emploi hors macro est une erreur. Un garde-fou `_macrosInExpansion` rejette une macro qui s'appelle elle-meme au lieu de developper a l'infini. Verifie : `EXITM` conditionnel rendant `AA` puis `AA BB` selon le drapeau, sortie a travers un `REPEAT`, macro imbriquee rendant `11 22 33`, `REPEAT` dans une macro rendant `05 05 05`, recursion rejetee sans blocage, et `coverage_all.asm` inchange a 892 octets malgre le changement de moment de resolution des conditionnelles. 127 tests verts dont 5 nouveaux, non-regression octet a octet inchangee.
- 2026-07-19 : **comparaison directe avec le moteur C**, enfin possible, l'executable de reference `xasm2026-1-2.exe` ayant ete verse dans `Reference/C/`. Les deux assembleurs ont ete lances dans des repertoires isoles avec des arguments strictement identiques, sur huit sources : `SAMPLE1` a `SAMPLE5`, `REGISTER`, `TMAP2020` et `VOGUE`. **Le code machine est identique dans les huit cas** (64, 77, 25, 6, 30, 4154, 1744 et 14433 octets). C'est une validation d'une autre nature que les goldens commites, qui sont des instantanes figes : ici les deux moteurs sont confrontes l'un a l'autre, et la comparaison couvre `SAMPLE1` a `SAMPLE4` que les goldens n'incluaient pas. Le `.uu` differe par deux points, une ligne finale `size` que nous emettons et que le `-B` integre au moteur C n'emet pas, et le bourrage du dernier bloc ; ce n'est pas une divergence involontaire mais le choix documente au journal du 2026-06-04 de reproduire `uuselfx.c` et les sorties DOSBox, et les deux fichiers decodent vers le meme objet. A noter que le script `tools/compare_with_xasm2026_1_1.ps1` rapporte des tailles trompeuses parce qu'il reutilise ses repertoires et y recopie les sorties pre-existantes des exemples : c'est la comparaison isolee qui fait foi.
- 2026-07-19 : ajout des exemples `SAMPLE6` a `SAMPLE9` dans `Exemples/SAMPLES`, illustrant les fonctionnalites ajoutees en 2026-4 : generation de donnees (`SET`, `REPEAT` generatif, `IRP`, `IRPC`, imbrication), macros avancees (portee anonyme par `LOCAL` nu, `EXITM`, macros imbriquees, `REPEAT` dans un corps), expressions (bases numeriques, operateurs `^ ~ << >>` et comparaisons, `LOW`/`MID`/`HIGH`, compteur `*`) et placement (`ALIGN`/`EVEN`, `DZ`, `PRE`, `ASSERT`/`WARNING`, `PHASE`/`DEPHASE`, `NOLIST`). Ces directives etant absentes du moteur C, les quatre fichiers **ne s'assemblent pas avec `xasm2026-1`** — verifie — et sont donc exclus de la comparaison avec la reference ; leurs octets sont verrouilles par `SampleAssemblyTests`. La documentation `Documentation/Documentation_XASM2026-4_PC-E500S.md` est mise a jour : options `-U` et `-?`, section 5.1 sur les operateurs et leurs precedences, section 6.1 sur les directives ajoutees, section 6.2 decrivant les exemples, et section 12 refondue avec la comparaison directe. Verifie : 134 tests verts dont 7 nouveaux.
- 2026-07-19 : regeneration propre des `.docx` et inversion de la logique du generateur. `python-docx` etant desormais installe et les deux ressources externes presentes, `tools/build_documentation.py` a pu produire les `.docx` a partir des `.md`. Mais un piege a ete decouvert avant de le lancer : le generateur **reconstruisait aussi les `.md` depuis un gabarit code en dur**, lequel ignorait les 140 lignes de documentation ajoutees le jour meme ; l'executer tel quel les aurait effacees en silence. La logique est donc inversee : les `.md`, versionnes et relus, font foi, et le generateur ne produit plus par defaut que les `.docx`. L'ancien comportement reste accessible via `--rebuild-markdown`, avec un avertissement explicite, pour le cas ou l'on voudrait repartir du gabarit. Verifie apres regeneration : les `.md` sont inchanges (800 lignes), seuls les deux `.docx` sont modifies, et leur contenu ne porte plus aucune occurrence de `2026-3` ni de `Codex` tout en incluant les nouvelles sections (`Directives ajoutees`, `SAMPLE6`, `-U`, `PHASE`).
- 2026-07-19 : correction de `tools/compare_with_xasm2026_1_1.ps1`, dont les deux defauts produisaient des resultats **trompeurs** plutot qu'en echec. (1) `Copy-ExampleFolder` recopiait chaque dossier d'exemple **avec ses sorties commitees** : une sortie que le run ne regenerait pas etait comparee a leur place et pouvait passer pour identique. Le dossier de travail est desormais purge de toutes les extensions produites (`.obj`, `.lst`, `.hex`, `.s19`, `.map`, `.d`, `.uu`, `.txt`, `.err`) avant l'assemblage, si bien qu'un fichier absent signifie reellement « non produit par ce run ». (2) Le dossier de travail etait nomme d'apres le dossier d'exemple et non d'apres la source : les cinq `SAMPLE*` partageaient `SAMPLES/` et s'ecrasaient mutuellement, ne laissant sur disque que la derniere execution — c'est ce qui rendait toute inspection a posteriori trompeuse. Chaque source recoit maintenant son propre dossier (`SAMPLES_SAMPLE1`, `SAMPLES_SAMPLE2`, ...). S'y ajoutent une colonne `MissingOutputs` et un avertissement nommant les fichiers qu'un run n'a pas produits, ainsi qu'un journal `_xasm.log` par execution, la sortie des assembleurs etant auparavant purement jetee. Verifie : les huit sources rapportent `Same=True` avec des tailles identiques, l'inspection des dossiers isoles donne exactement le meme verdict que le rapport, et un essai avec `SAMPLE6` — qui echoue sur la reference puisqu'il emploie des directives 2026-4 — remonte bien `MissingOutputs = reference .uu` et `ReferenceExit = 1`, la source suivante restant propre.
